name: email_triage_env

description: Email classification and triage OpenEnv environment using FastAPI and LLM agents.

entrypoint:
module: server.app:app

endpoints:
reset: /reset
step: /step
state: /state

observation_space:
type: object

action_space:
type: object

reward_range:
min: -0.2
max: 1.0
